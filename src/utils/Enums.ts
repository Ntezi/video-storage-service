export enum CartType {
	PURCHASE_CART_TYPE = 1, // When the cart is created for purchase.
	SALE_CART_TYPE = 2, // When the cart is created for sale.
	REFUND_CART_TYPE = 3, // When the cart is created for refund.
	PROFORMA_CART_TYPE = 4, // When the cart is created for pro form.
}
